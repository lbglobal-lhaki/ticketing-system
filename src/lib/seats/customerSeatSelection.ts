/**
 * Customer checkout seat map. Flip to true to show `/checkout/[id]/seats`
 * again. The page, map, and admin seat pricing stay in the repo.
 */
export const CUSTOMER_SEAT_SELECTION_ENABLED = false;
